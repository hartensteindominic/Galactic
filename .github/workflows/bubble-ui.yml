name: Legacy HyperStream Bubble UI

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The automatic HyperStream index rewriter is retired so Galactic x402 licensing stays intact."
